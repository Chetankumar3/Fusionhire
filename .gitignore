# Python
__pycache__/
*.pyc
.venv/
venv/

# Node
node_modules/

# Pipeline intermediate state (regenerated every run)
shared_memory/db/
shared_memory/parsed_jsons/

# Keep: parsers/resume_parser/raw_json/ (committed so the resume source works
# without Node) and shared_memory/output_*.json (the produced sample output).
personal/